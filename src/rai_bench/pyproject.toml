[tool.poetry]
name = "rai-bench"
version = "0.1.0"
description = "Package for running and creating benchmarks."
authors = ["jmatejcz <jakub.matejczyk@robotec.ai>", "Magdalena Kotynia <magdalena.kotynia@robotec.ai>"]
readme = "README.md"

packages = [
    { include = "rai_bench", from = "." },
]
[tool.poetry.dependencies]
python = "^3.10"
inflect = "7.5.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
