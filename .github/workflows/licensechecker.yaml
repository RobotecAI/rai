name: Check License Lines
on: [pull_request]
jobs:
  check-license-lines:
    runs-on:
      - self-hosted
      - robo-pc-066
    container:
      image: ubuntu:24.04

    steps:
      - uses: actions/checkout@master
      - name: Check License Lines
        uses: kt3k/license_checker@v1.0.6
