name: "Require rai_core pyproject change when code changes"
on:
  pull_request:
    branches:
      - main
      - development
    paths:
      - 'src/rai_core/**'
      - 'src/rai_core/pyproject.toml'

jobs:
  check-rai-core-pyproject:
    name: Require version bump if rai_core changed
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install tomli
        run: |
          pip install tomli

      - name: Verify rai_core pyproject changed
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail

          echo "Base: ${BASE_SHA}"
          echo "Head: ${HEAD_SHA}"

          # List changed files between base and head
          CHANGED_FILES=$(git diff --name-only "${BASE_SHA}" "${HEAD_SHA}")
          printf "Changed files:\n%s\n" "${CHANGED_FILES}"

          # Detect changes within src/rai_core/
          if ! printf '%s\n' "${CHANGED_FILES}" | grep -E '^src/rai_core/' >/dev/null 2>&1; then
            echo "No changes detected in src/rai_core. Skipping check."
            exit 0
          fi

          echo "Changes detected in src/rai_core. Verifying src/rai_core/pyproject.toml is updated..."
          echo "Reading versions from src/rai_core/pyproject.toml (for context)..."
          BASE_VERSION=$(git show "${BASE_SHA}:src/rai_core/pyproject.toml" 2>/dev/null | python -c "import tomli, sys; data=sys.stdin.read(); print(tomli.loads(data)['project']['version']) if data.strip() else exit(1)" 2>/dev/null || true)
          HEAD_VERSION=$(git show "${HEAD_SHA}:src/rai_core/pyproject.toml" 2>/dev/null | python -c "import tomli, sys; data=sys.stdin.read(); print(tomli.loads(data)['project']['version']) if data.strip() else exit(1)" 2>/dev/null || true)
          echo "Base version: ${BASE_VERSION:-<not found>}"
          echo "Head version: ${HEAD_VERSION:-<not found>}"

          if [ "${BASE_VERSION}" = "${HEAD_VERSION}" ]; then
            echo "ERROR: pyproject.toml modified but version was not bumped."
            exit 1
          fi

          echo "✅ Version bump detected: ${BASE_VERSION} -> ${HEAD_VERSION}"
          exit 0

          echo "ERROR: Files in src/rai_core changed but src/rai_core/pyproject.toml was not modified in this PR."
          echo "Update the version in the pyproject.toml file and commit the changes according to changes with respect to Semantic Versioning."
          echo "Bump patch if a bug fix is introduced, minor if a feature is added, and major if a breaking change is added."
          echo "Disregard the version bump only if you know what you are doing."
          exit 1
